import { TopDestinationsComponent } from './top-destinations.component';

describe('TopDestinationsComponent', () => {

  it ('Exists', () => {
    expect(TopDestinationsComponent).toBeDefined();
  });

});
